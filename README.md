# marketplace-mock-api

## running
```
npm install
npm start
```
By default the app starts in port `3001`

## endpoints

```
/node-connections
/topology/:streamId
/streams
/streams/:id
/streams/:id/validation
```
